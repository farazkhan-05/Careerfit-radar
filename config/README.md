# Local Configuration

Use `candidate_profile_input.json` for resume-derived preferences and profile hints.

1. Copy the structure from `candidate_profile_input.example.json`.
2. Paste your generated JSON into `candidate_profile_input.json`.
3. Keep it valid JSON.

`candidate_profile_input.json` is ignored by git because it may contain personal resume details.
